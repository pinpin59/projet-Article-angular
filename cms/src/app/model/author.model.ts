export class Author {
    id: string;
    name: string;
    bio: string;
  
    constructor(id: string, name: string, bio: string) {
      this.id = id;
      this.name = name;
      this.bio = bio;
    }
  }